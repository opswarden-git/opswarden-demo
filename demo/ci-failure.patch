diff --git a/src/deployment-health.ts b/src/deployment-health.ts
--- a/src/deployment-health.ts
+++ b/src/deployment-health.ts
@@ -14,7 +14,7 @@ export function assessDeployment(signal: DeploymentSignal): DeploymentState {
     throw new RangeError("errorRatePercent must be a finite positive number");
   }
 
-  if (!signal.ready || signal.errorRatePercent > 5) {
+  if (!signal.ready || signal.errorRatePercent > 50) {
     return "unhealthy";
   }
 

